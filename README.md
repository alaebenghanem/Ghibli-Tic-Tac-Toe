# ✦ Ghibli Tic-Tac-Toe ✦

A cute, lightweight desktop Tic-Tac-Toe game styled with warm, cozy Ghibli vibes. 

Play as **Calcifer** (from *Howl's Moving Castle*) or **Jiji** (from *Kiki's Delivery Service*).

## Features
- **Cozy Design**: Sunny colors, rounded corners, custom sprites, and smooth animations.
- **Ghibli player**: **Calcifer** (from *Howl's Moving Castle*) as **X** and **Jiji** (from *Kiki's Delivery Service*) as **O**.
- **Window Dragging**: Click and drag the top title bar to move the window.
- **Window Controls**: Custom buttons in the title bar to Restart (↻), Minimize (─), and Close (✕) the window.


## How to Run

1. **Install dependencies** (first time only):
   ```bash
   npm install
   ```

2. **Start the game in development mode**:
   ```bash
   npm run tauri dev
   ```

3. **Build the installer for production**:
   ```bash
   npm run tauri build
   ```
"# Ghibli-Tic-Tac-Toe" 
